Installation
Clone the selectionSaver to C:\Users<user>\Documents\maya\scripts or to a folder included in PYTHONPATH.

Build Command
```
import selectionSaver.ui as ss
reload(ss)
ssui = ss.UI()
ssui.buildUI()
```
